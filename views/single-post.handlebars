{{> post-info post}}
  {{#if loggedIn}}
    <form class="comment-form">
      <div>
        <textarea id="comment-body" name="comment-body" maxlength="400"></textarea>
      </div>
      <div>
        <button type="submit">Add Comment</button>
      </div>
    </form>
  {{/if}}
{{> comments post.comments}}
  {{#if loggedIn}}
    <script src="/javascript/comment.js"></script>
  {{/if}}